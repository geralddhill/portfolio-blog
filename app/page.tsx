import Hero from "@/ui/landing/Hero";

export default function Home() {
    return (
        <main className="flex flex-col items-center justify-center w-full">
            <Hero/>
            <p className="h-100">This page is still under construction.</p>
        </main>
      );
}
